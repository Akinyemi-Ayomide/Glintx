// const Loader = () => {
 // return (
    // <div className={styles.dots}>
     //  {Array.from({ length: 9 }).map((_, i) => (
       //  <div key={i}></div>
      // ))}
    // </div>
 // );
// };

// export default Loader;
